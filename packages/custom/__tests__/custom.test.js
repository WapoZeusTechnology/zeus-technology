// 'use strict';

// const customSsp = require('..');

describe("custom-ssp", () => {
  it("needs tests", () => {
    // First, let's try to verify that we can inject ourself into `zeus-lib`.
  });
});
